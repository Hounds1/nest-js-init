generator client {
  provider     = "prisma-client"
  output       = "../src/generated/prisma"
  moduleFormat = "cjs"
}

datasource db {
  provider = "postgresql"
}

model TeamMember {
  memberId Int    @id @default(autoincrement())
  name     String
  age      Int
}